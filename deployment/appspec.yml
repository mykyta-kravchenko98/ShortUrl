version: 0.0
os: linux
files:
  - source: app.zip
    destination: ~/smurl
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      runas: ubuntu
      timeout: 300
  AfterInstall:
    - location: scripts/after_install.sh
      runas: ubuntu